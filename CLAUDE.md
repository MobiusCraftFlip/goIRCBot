# Database
every time a change is made to the database a migration should be created in the db_migrations folder/ 

# Docs
## Modules
All Modules should store it's documentation in docs/modules/<ModuleName>.md 
This will store information such as it's commands and it's configuation options. 
Whenever a module is added or edited, the documentation for that module should also be updated to reflect the changes.